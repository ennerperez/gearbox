name: Pull Request
on:
  pull_request:

env:
  GITHUB_TOKEN: ${{ github.token }}

jobs:
  test:
    name: Test
    uses: ./.github/workflows/test.yml

  analyze:
    name: Analyze
    uses: ./.github/workflows/analyze.yml

